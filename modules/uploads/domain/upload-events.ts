/**
 * Upload event constants.
 *
 * Added here (not in GlobalEvents) to keep uploads module self-contained.
 * If cross-module consumption is needed later, migrate to event-registry.
 */
export const FILE_UPLOADED = 'file.uploaded';
export const FILE_DELETED = 'file.deleted';
